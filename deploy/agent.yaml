# 节点代理示例配置
server:
  addr: "127.0.0.1:7443"
node:
  name: ""              # 留空则取主机名
  partition: "default"
  labels:
    zone: "lab"
collectors:
  interval: "10s"       # 采样与心跳周期
log:
  level: "info"
